# Grid-pattern-code
This tool converts your normal sentence into a secure coded format by hiding a specific single letter.  Without knowing the hidden letter, the encoded message cannot be correctly decoded.
